CREATE TABLE employer (
    id SMALLSERIAL PRIMARY KEY,
    name VARCHAR(120) NOT NULL UNIQUE,
    abrev VARCHAR(80) NOT NULL UNIQUE,
    status VARCHAR(120) NOT NULL,
    SIREN INT,
    SIRET INT,
);
